# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.egg
build/
dist/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Word temp files
~$*.docx

# Build / output (generated at runtime, not source)
/outputs/
# F-0.5: /output/ (单数) 此前未被忽略, 里面已存放其他项目的客户材料
# (docx/pptx/bid), 且 F-1A 的 payload 会写到 output/frontend/。
# 不忽略它就是在制造"一次 git add -A 把客户数据推进仓库"的路径。
/output/
/tmp/
*.zip

# Data (scraped quota DB + raw JSON, too large for git)
/data/
